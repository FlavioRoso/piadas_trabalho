export default class Piada {
    constructor(id, titulo, texto, categoria_id)
    {
        this.id = id;
        this.titulo = titulo;
        this.texto = texto;
        this.categoria_id = categoria_id;
    }
}
